server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smart_recruit_system?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
  resources:
    static-locations: classpath:/templates  #访问静态页面
# Mybatis-plus配置内容
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
logging.level.com.zhbit.smartrecruit.dao : debug